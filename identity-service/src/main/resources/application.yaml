
# information url
server:
  port: 8080
  servlet:
    context-path: /identity


# Connect to Database
spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/identity_service"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name=identity-service:
